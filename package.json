{
  "name": "mons-dot-link",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "@mons/shared": "file:cloud/functions/shared",
    "@solana/wallet-adapter-base": "^0.9.27",
    "@types/node": "^26.1.2",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "bs58": "^6.0.0",
    "ethers": "^6.17.0",
    "firebase": "^12.17.0",
    "glicko2": "^1.2.1",
    "mons-rules": "0.3.4",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "react-icons": "^5.7.0",
    "react-scripts": "5.0.1",
    "styled-components": "^6.4.4",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "remotely": "node scripts/remotely.js",
    "build": "react-scripts build",
    "repo-clean": "bash scripts/repo-clean.sh",
    "format": "prettier --write . --ignore-unknown",
    "format:check": "prettier --check . --ignore-unknown",
    "deploy:firebase": "npm --prefix cloud/functions install && cd cloud && firebase deploy --only functions,firestore:rules,firestore:indexes,database",
    "deploy:allFunctionsBatched": "npm --prefix cloud/functions install && npm --prefix cloud/functions run deploy:safe --",
    "deploy:monsRulesFunctions": "npm --prefix cloud/functions install && npm --prefix cloud/functions run deploy:safe -- projectProfileEventPrizesOnPrizeWritten projectProfileEventPrizesOnMergeTargetWritten getNfts withdrawEventPrize verifySolanaAddress verifyEthAddress verifyAppleToken completeXRedirectAuth automatch updateRatings startMatchTimer claimMatchVictoryByTimer resolveWagerOutcome createEvent joinEvent postponeEventStart removeEventParticipant disqualifyEventMatchWinners syncEventState processEventProgress processEventProgressFallback",
    "deploy": "node --experimental-strip-types scripts/deploy-amplify.ts",
    "latest:root": "npx --yes npm-check-updates@latest -u --peer && npm install --legacy-peer-deps",
    "latest:functions": "npx --yes npm-check-updates@latest -u --peer --packageFile cloud/functions/package.json && npm install --prefix cloud/functions",
    "latest:admin": "npx --yes npm-check-updates@latest -u --peer --packageFile cloud/admin/package.json && npm install --prefix cloud/admin",
    "latest": "npm run latest:root && npm run latest:functions && npm run latest:admin"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ],
    "rules": {
      "no-mixed-operators": [
        "error",
        {
          "groups": [
            [
              "&&",
              "||"
            ]
          ],
          "allowSamePrecedence": true
        }
      ]
    }
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
