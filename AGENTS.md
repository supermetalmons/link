Avoid code comments except for TODOs or essential context.

Use the routine release path in `scripts/deploy-cloudflare.md` by default. Prepare validation and candidate uploads first, then promote and verify the affected behavior. Routine releases have no overall time limit: necessary work, provider latency, propagation, and verification retries should take the time they need. Do not add verification-only waits or observation windows longer than 60 seconds, or chain short waits into extended monitoring; prefer no artificial waits and finish once the required checks pass. Do not stop or request renewed approval solely because a release takes more than 60 seconds. Do not add write freezes or Queue pauses to ordinary code, UI, or prize-catalog releases. Use coordinated maintenance only for a concrete schema, state-compatibility, resource-lifecycle, or incident requirement, and explain that requirement before applying its controls.
